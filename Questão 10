let ano = parseInt(prompt("Digite um ano:"));

// Ano bissexto: múltiplo de 4, mas não de 100. Ou múltiplo de 400.
if ((ano % 4 === 0 && ano % 100 !== 0) || (ano % 400 === 0)) {
    console.log(ano + " é bissexto.");
} else {
    console.log(ano + " não é bissexto.");
}
